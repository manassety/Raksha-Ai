"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SendDirectSms = void 0;
var _reactNative = require("react-native");
const LINKING_ERROR = `The package 'react-native-send-direct-sms' doesn't seem to be linked. Make sure: \n\n` + _reactNative.Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo Go\n';
const SendSms = _reactNative.NativeModules.SendDirectSms ? _reactNative.NativeModules.SendDirectSms : new Proxy({}, {
  get() {
    throw new Error(LINKING_ERROR);
  }
});
const SendDirectSms = async (mobileNumber, bodySMS) => {
  return new Promise(async (resolve, reject) => {
    if (mobileNumber) {
      try {
        // @ts-ignore
        const granted = await _reactNative.PermissionsAndroid.request(_reactNative.PermissionsAndroid.PERMISSIONS.SEND_SMS);
        if (granted === _reactNative.PermissionsAndroid.RESULTS.GRANTED) {
          SendSms.sendDirectSms(mobileNumber, bodySMS);
          resolve('SMS sent'); // Resolve the promise if SMS sent successfully
        } else {
          reject('SMS permission denied'); // Reject the promise if permission is denied
        }
      } catch (error) {
        reject(error); // Reject the promise if an error occurs
      }
    } else {
      reject('Invalid mobile number'); // Reject the promise for an invalid mobile number
    }
  });
};
exports.SendDirectSms = SendDirectSms;
//# sourceMappingURL=index.js.map